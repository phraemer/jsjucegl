## JUCE basic plugin that uses OpenGL and timed repainting

On Windows:

- To generate a Visual Studio 2019 solution run `generate.bat`
- To build the plugin outside Visual Studio run `build_debug.bat`
